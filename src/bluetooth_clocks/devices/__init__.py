"""Device-specific Bluetooth clock support.

Each submodule in this package implements support for a specific type of
Bluetooth clock.

Each class in these submodules implements support for a specific model
Bluetooth clock.

For instance, the :mod:`bluetooth_clocks.devices.thermopro` module has classes
:class:`bluetooth_clocks.devices.thermopro.TP358` and
:class:`bluetooth_clocks.devices.thermopro.TP393` for the ThermoPro TP358 and
TP393, respectively.
"""
